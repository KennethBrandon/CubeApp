# User Rules

- Do not use the browser tool (`browser_subagent`) or open local URLs automatically.
- I will handle the browser and check console logs myself.
- Only use browser tools if I explicitly ask you to.
